import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lizeth.ruralincidents',
  appName: 'Incidentes Rurales',
  webDir: 'dist/rural-incidents/browser'
};

export default config;
